#!/usr/bin/env bash

set -euo pipefail

usage() {
  printf 'usage: .system/new-paper-note-project <project-id>\n' >&2
}

fail() {
  printf 'new-paper-note-project: %s\n' "$1" >&2
  exit 1
}

script_path="${BASH_SOURCE[0]}"
[[ ! -L "$script_path" ]] || fail "script path check failed"
script_dir="$(cd -P -- "$(dirname -- "$script_path")" && pwd)" || \
  fail "repository root is unavailable"
repo_root="$(git -C "$script_dir/.." rev-parse --show-toplevel 2>/dev/null)" || \
  fail "repository root is unavailable"
repo_root="$(cd -P -- "$repo_root" && pwd)" || \
  fail "repository root is unavailable"
[[ "$script_dir" == "$repo_root/.system" ]] || fail "script path check failed"
cd -- "$repo_root"

"$repo_root/.system/doctor"

if (( $# != 1 )); then
  usage
  exit 2
fi

project_id="$1"
case "$project_id" in
  AGENTS.md|agents.md|paper-lecture-notes|projects|shared|system|inbox|draft|out|workspace|source|records|tools|review|build|cache|tmp|logs|locks|assets|figures|generated|software-output|readme.md|bootstrap|doctor|repository-mode|sync-mode-excludes|verify-private-remote|new-book-project|new-paper-project|new-paper-note-project)
    fail "project id is reserved"
    ;;
esac

[[ "$project_id" =~ ^[a-z0-9]+(-[a-z0-9]+)*$ ]] || \
  fail "project id is invalid"
[[ "$project_id" != */* && "$project_id" != *\\* ]] || \
  fail "project id is invalid"

projects_root_path="$repo_root/paper-lecture-notes"
[[ -d "$projects_root_path" && ! -L "$projects_root_path" ]] || \
  fail "project root check failed"
projects_root="$(cd -P -- "$projects_root_path" && pwd)" || \
  fail "project root check failed"
[[ "$projects_root" == "$projects_root_path" ]] || \
  fail "project root check failed"

target="$projects_root/$project_id"
[[ "$(dirname -- "$target")" == "$projects_root" ]] || \
  fail "target path check failed"
[[ ! -e "$target" && ! -L "$target" ]] || fail "target already exists"

created_target=0
keep_target=0
cleanup() {
  status=$?
  trap - EXIT

  if (( created_target == 1 && keep_target == 0 )) && \
    [[ -d "$target" && ! -L "$target" ]]; then
    rm -f -- \
      "$target/.workspace/project-id" \
      "$target/.workspace/system-version" 2>/dev/null || true
    rmdir -- \
      "$target/.workspace/source" \
      "$target/.workspace/records" \
      "$target/.workspace/tools" \
      "$target/.workspace/review" \
      "$target/.workspace/build" \
      "$target/.workspace/cache" \
      "$target/.workspace/tmp" \
      "$target/.workspace/logs" \
      "$target/.workspace/locks" \
      "$target/inbox" \
      "$target/draft" \
      "$target/out" 2>/dev/null || true
    rmdir -- "$target/.workspace" 2>/dev/null || true
    rmdir -- "$target" 2>/dev/null || true
  fi

  exit "$status"
}
trap cleanup EXIT

umask 077
mkdir -- "$target" 2>/dev/null || fail "project creation failed"
created_target=1
mkdir -- \
  "$target/inbox" \
  "$target/draft" \
  "$target/out" \
  "$target/.workspace" \
  "$target/.workspace/source" \
  "$target/.workspace/records" \
  "$target/.workspace/tools" \
  "$target/.workspace/review" \
  "$target/.workspace/build" \
  "$target/.workspace/cache" \
  "$target/.workspace/tmp" \
  "$target/.workspace/logs" \
  "$target/.workspace/locks" 2>/dev/null || fail "project creation failed"

head_commit="$(git rev-parse --verify 'HEAD^{commit}' 2>/dev/null)" || \
  fail "HEAD commit is unavailable"
[[ "$head_commit" =~ ^[0-9a-f]{40,64}$ ]] || \
  fail "HEAD commit is unavailable"
if ! { printf '%s\n' "$project_id" >"$target/.workspace/project-id" &&
       printf '%s\n' "$head_commit" >"$target/.workspace/system-version"; } \
  2>/dev/null; then
  fail "project identity could not be recorded"
fi

relative_target="paper-lecture-notes/$project_id"
mode="$(git config --local --get books.repositoryMode 2>/dev/null)" || \
  fail "repository mode check failed"
git check-ignore --no-index -q -- "$relative_target/" >/dev/null 2>&1 && \
  ignore_status=0 || ignore_status=$?
case "$mode:$ignore_status" in
  public:0|private:1)
    ;;
  public:*)
    fail "public private-path ignore check failed"
    ;;
  private:*)
    fail "private path staging check failed"
    ;;
  *)
    fail "repository mode check failed"
    ;;
esac

keep_target=1
printf 'new-paper-note-project: ok\n'
