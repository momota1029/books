# Mode-dependent privacy and build-artifact patterns are managed clone-locally
# in .git/info/exclude by .system/sync-mode-excludes.
# Do not add broad patterns that match project .workspace files: verified
# private mode must be able to checkpoint every permitted .workspace artifact.
